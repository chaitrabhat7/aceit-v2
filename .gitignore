.env
venv/
__pycache__/
*.pyc
chroma_db/
*.sqlite3

# Google Cloud Vision credentials — never commit
.streamlit/secrets.toml
*service-account*.json
gcp-*.json

# Local anonymous usage log — durable copy lives in the Google Sheet
usage_log.jsonl
